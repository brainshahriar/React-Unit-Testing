import React from 'react';
import Button from '../Button/Button';

const ComponentTest = () => {
    return (
        <div>
            <Button/>
        </div>
    );
};

export default ComponentTest;